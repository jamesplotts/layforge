# V1 Web Client

The default Slave client: narrative scrollback, schema-driven stat panel,
dice tray, push-to-talk voice input. No LLM credentials, no rules engine —
renders what Master sends over the protocol.

Not yet implemented — see [`docs/design.md`](../../docs/design.md) §4.
